# Toolchain is stable 1.88.0. Nightly-only rustfmt keys are omitted so
# `cargo fmt --all -- --check` matches this file without unstable-feature warnings.
max_width = 120
fn_params_layout = "Tall"
reorder_imports = true
use_field_init_shorthand = true
